thought_process_prompt = f"""
<!-- PROTECTED_BASELINE_START -->
请按照以下步骤思考，并在思考过程中展示你的推理过程：
步骤1：分析用户描述的化工流程
- 识别主要设备单元和模拟类型，确定是单元模拟还是流程模拟，是否需要包含循环物流
- 理解物料流向
- 确定关键操作参数

步骤2：设计流程结构
- 识别流程是否包含循环，当前流程包含循环时，请遵循"分步设计、逐步收敛"的原则，按如下子步骤执行，先设计开环流程，调用模拟工具运行，确保各单元操作收敛后，再逐步添加循环流股，最终形成闭环：
1. **流程分解**：先将整个流程分解为多个串联步骤（如物料混合→反应→分离→精馏等）
2. **开环设计**：从第一步开始设计，先生成一个不含循环的开环流程配置
3. **逐步收敛**：
   - 调用模拟工具运行当前配置
   - 检查收敛情况，确保当前步骤模拟成功
   - 根据模拟结果调整参数，或添加下一步设备
   - 重复此过程，逐步扩展流程
4. **循环闭合**：当所有主要单元都添加完毕且收敛后，最后添加循环流股，形成闭环
5. **精馏塔特殊处理**：当任务涉及精馏塔严格计算时，必须按以下两步法执行，禁止直接一步输出 RadFrac 配置：
     **第一步：使用DSTWU 简捷设计获取初值
     a. 先生成 DSTWU 简捷设计塔的配置，输入分离要求（轻关键组分回收率、重关键组分回收率或馏出物纯度等）。
     b. 调用模拟工具运行 DSTWU 配置。
     c. 获取DSTWU的运行结果，包括：
        - 最小回流比
        - 实际回流比（通常取最小回流比的1.1-1.5倍）
        - 理论塔板数
        - 进料板位置
        - 冷凝器/再沸器热负荷
     d. 明确提示“以下为 DSTWU 初值结果，将作为 RadFrac 严格模型的初值”。
     **第二步：基于DSTWU初值搭建RadFrac 严格模型**
     e. 基于 DSTWU 获取的初值，生成 RadFrac 严格模型的配置。
     f. 在RadFrac 配置中，根据分离要求判断是否设置设计规定：
        - 若用户指定了馏出物纯度、釜残物纯度、回收率等目标，则配置RadFrac配置中的DESIGN_SPEC_DATA设计规定模块，并设置相应的 Vary 变量（如回流比、馏出物流量等），如果有个设计目标，则需添加多条设计规定配置，比如塔顶和塔底的产品纯度都要要求，则需添加2条设计规定。
        - 若用户未指定分离目标，仅提供了操作条件（如回流比、塔板数），则直接运行，无需设计规定。
     g. 将 DSTWU 的初值（塔板数、进料位置、回流比初值）作为 RadFrac 的初始参数填入。
  
     **禁止行为（必须遵守）**：
     **关于 DSTWU 初值获取**：
     - 禁止在未运行 DSTWU 获取初值的情况下直接生成 RadFrac 配置。
     - 禁止在 RadFrac 中随意填写塔板数、回流比等关键参数（必须基于 DSTWU 结果）。
     
     **关于设计规定的强制使用条件**：
     - **当客户明确提出以下任一要求时，必须在RadFrac配置中使用DESIGN_SPEC_DATADesign Specs 设计规定模块，如果有个设计目标，则需添加多条设计规定配置，比如塔顶和塔底的产品纯度都要要求，则需添加2条设计规定。禁止以“手动调整参数”的方式替代**：
       * 指定馏出物（塔顶）某组分的纯度（如“塔顶甲醇纯度≥99.5%”）
       * 指定釜残物（塔底）某组分的纯度（如“塔底水中有机物含量≤0.1%”）
       * 指定某关键组分的回收率（如“轻关键组分回收率98%”）
       * 指定馏出物与进料的比例（如“馏出物/进料 = 0.6”）
       * 指定塔顶或塔底的流量或组成达到某一目标值
       * 任何涉及“达到XX分离效果”“满足XX指标”“确保XX纯度”等分离目标描述
     
     - **以下情况可豁免设计规定配置**：
       * 客户仅提供了操作参数（回流比、塔板数、进料位置等），未提出具体分离目标
       * 客户明确说明“不需要设计规定”或“直接给定操作条件运行”
     
     - **判断原则**：当存疑时，默认视为需要设计规定，优先配置 Design Specs 模块以确保满足分离要求。


步骤3：验证配置逻辑，必须符合JSON Schema的格式和检查要求
- 检查端口连接的正确性，端口关系必须符合JSON Schema的block_connections枚举定义，不要编造P1(OUT)等新的端口配置
- 验证冷凝器类型与流股的匹配
- 确保反应器配置合理，需检查并确保配置的反应物与产物的总原子数守恒。
- 确保物料平衡、质量平衡。

步骤4：生成最终配置。
- 严格按照JSON Schema格式
- 确保所有必填字段完整
- 验证配置逻辑一致性
<!-- PROTECTED_BASELINE_END -->

<!-- OPTIMIZATION_ZONE_START: 循环流程错误恢复 -->
<!-- 此区域包含针对循环流程实际失败模式的补充策略 -->
<!-- OPTIMIZATION_ZONE_END: 循环流程错误恢复 -->

<!-- OPTIMIZATION_ZONE_START: 精馏塔错误恢复 -->
<!-- 此区域包含针对精馏塔实际失败模式的补充策略 -->
<!-- OPTIMIZATION_ZONE_END: 精馏塔错误恢复 -->

<!-- OPTIMIZATION_ZONE_START: 反应器错误恢复 -->
<!-- 此区域包含针对反应器实际失败模式的补充策略 -->
<!-- OPTIMIZATION_ZONE_END: 反应器错误恢复 -->

<!-- OPTIMIZATION_ZONE_START: 其他单元设备错误恢复 -->
<!-- 此区域包含针对其他单元设备实际失败模式的补充策略 -->
<!-- OPTIMIZATION_ZONE_END: 其他单元设备错误恢复 -->

<!-- OPTIMIZATION_ZONE_START: 通用错误恢复 -->
1. 当出现“撕裂流股不可以是流程中的进料流股或产品流股”错误时，立即检查并修改撕裂流股（Tear）的设定，确保其不是任何进料流股（Feed）或产品流股（Product），通常需要重新指定一个内部循环流股作为撕裂流股。
2. 当出现“已生成导致 SEP2 模块方程无解的规定”错误时，检查SEP2模块的输入规定（如分流率、产品纯度），确保其物料平衡可行，避免出现总和超过100%或负值等不合理规定，必要时放宽或调整规定值。
3. 当run_simulation返回“NO BLOCK PARAGRAPH HAS BEEN ENTERED FOR IT”错误时，立即检查blocks配置，确保流程图中出现的所有设备模块（如错误中的S1）都在blocks配置中有对应的详细参数定义，补充遗漏的模块配置。
<!-- OPTIMIZATION_ZONE_END: 通用错误恢复 -->
"""
